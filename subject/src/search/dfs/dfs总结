1. 在dfs遍历时，如果要防止回访本次路劲已遍历过的位置，可以采取vis数组来记录，但是值得注意的一点是，要分清在何时记录，
    何时清除，比如如果当前路径访问完成，但是还没有找到解时，回溯时一定要重设vis，否则会导致下一条路径遍历的时候由于
    前一次路径寻找时vis过的位置不可达，造成错误。——所以总结一点，vis记录和vis恢复总是成对出现的。
2. 如果需要记录中间遍历的值到一个非基本类型中，由于非基本类型都是传递的引用类型，在外层循环时，虽然remove后接了add操作，
    恢复原有数据，但是循环位置对应的数据已经发生了变化，所以也会造成错误。——最好的办法是，通过copy方式传递非基本类型。
    这样下层栈对中间结果的操作和上层栈对中间结果的操作互不影响。